[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sam-gov-mcp"
version = "0.1.0"
description = "MCP server for SAM.gov entity registration, exclusion, and contract opportunity data"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "1102tools" }
]
keywords = ["mcp", "model-context-protocol", "sam.gov", "federal-contracts", "procurement", "exclusions", "debarment", "1102"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.urls]
Homepage = "https://1102tools.com"
Repository = "https://github.com/1102tools/sam-gov-mcp"
Issues = "https://github.com/1102tools/sam-gov-mcp/issues"

[project.scripts]
sam-gov-mcp = "sam_gov_mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/sam_gov_mcp"]
