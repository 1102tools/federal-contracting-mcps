# BLS API key (optional but recommended)
# Without key (v1): 25 queries/day, 25 series/query
# With key (v2): 500 queries/day, 50 series/query
# Register free at https://data.bls.gov/registrationEngine/
BLS_API_KEY=your-api-key-here
